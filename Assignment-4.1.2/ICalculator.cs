namespace Caleb_Marks_Assignment_4._1._2
{
    // List the four calculations that the Math class must include
    internal interface ICalculator
    {
        decimal Add(decimal first, decimal second);
        decimal Subtract(decimal first, decimal second);
        decimal Multiply(decimal first, decimal second);
        decimal Divide(decimal first, decimal second);
    }
}
